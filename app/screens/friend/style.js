import {
  StyleSheet,
} from 'react-native';

import { COLOR_ICON_LABEL } from '../../styles/common';

const styles = StyleSheet.create({
  container: {
    alignItems: 'center',
    flex: 1,
    marginTop: 20,
  },
  main:{
  	flex:1,
  },
});

export default styles;
